MealDB Backend API 🍽️

This project is an ASP.NET Core Web API that acts as a backend service for fetching meal and recipe data using TheMealDB public API. It is designed to be consumed by frontend applications such as Angular or Blazor.

 🚀 Features
- Fetch meals by category
- Fetch meals by area (cuisine)
- Search meals by name
- Retrieve detailed meal information including ingredients and instructions
- RESTful endpoints
- CORS enabled for frontend integration

 🛠️ Tech Stack
- ASP.NET Core Web API
- C#
- HttpClient
- TheMealDB API

📡 Sample Endpoints
- GET /api/meals/category/{category}
- GET /api/meals/area/{area}
- GET /api/meals/search/{name}

 ▶️ How to Run Locally
1. Clone the repository
2. Open the solution in Visual Studio
3. Restore NuGet packages
4. Run the application
5. Test endpoints using Postman or browser

 🌐 Deployment
The API is deployed and accessible for frontend applications.

